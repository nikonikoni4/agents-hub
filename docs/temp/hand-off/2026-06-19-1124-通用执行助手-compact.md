# Context Compact - 通用执行助手 - 2026-06-19T11:24:38.535373

## 原 Session
- session_id: 2cec124f-1cdd-47e5-b332-1ea6ecb2394b
- context_usage: 207K tokens

## 摘要
## 工作上下文总结

### 已完成的工作

1. **创建聊天历史记录 spec** - `docs/specs/2026-06-19-chat-history.md`，定义了群聊聚合消息和成员个人历史的存储模型、检索接口、数据结构

2. **前端历史界面支持工具调用信息** - 完整功能实现：
   - 后端：`session_parser.py` 新增 `ToolCallInfo` 和 `tool_calls` 字段
   - 后端：Schema 层（`group_chats.py`、`single_chat.py`）导入并使用统一的 `ToolCallInfo`
   - 后端：Service 层传递 `tool_calls` 字段
   - 前端：将 `ToolCallCard` 组件从 `single-chat` 迁移到 `shared/components`（符合架构规则）
   - 前端：`ChatHistoryPanel` 展示工具调用卡片

3. **重构 Codex session 解析器** - 修复了 `parse_codex_session()` 中工具调用解析：
   - 原来错误地处理 `tool_use` block（永远不会触发）
   - 改为处理顶层 `function_call` response_item
   - 通过 `call_id` 关联 `function_call` 和 `function_call_output`
   - 添加了测试用例验证

4. **修复 NOTIFICATION 消息保存问题** - 在 `_run_loop` 中增加 NOTIFICATION 消息保存到群聊历史的逻辑

### 当前状态

所有任务已完成，等待新的指令。

### 关键决策

- `ToolCallInfo` 统一定义在 `session_parser.py`（DRY 原则）
- `ToolCallCard` 放在 `shared/components`（feature 间不能直接依赖）
- Codex 工具调用格式与 Claude 不同：顶层 `function_call` vs assistant message 的 `tool_use` block

## 新 Session
- session_id: fbdb0e75-68a8-43bc-861d-ca50679a6874
