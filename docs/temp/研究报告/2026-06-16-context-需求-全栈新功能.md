# AI 完成"文件上传功能"所需上下文调研报告

## 元信息

- 调研日期：2026-06-16
- 调研任务：从代码出发，理解"前端文件上传发送给 Agent"功能需要哪些上下文
- 约束条件：不查看 `docs/specs/` 下的任何文档，只从代码推断

---

## 1. 探索日志

### 第 1 步：理解现有消息结构

**执行的命令**：
```bash
grep -r "class.*Message" agents_hub/core/foundation/
grep -r "AgentMessage" agents_hub/
```

**发现**：
- 找到 `agents_hub/core/foundation/message.py` 定义了 `AgentMessage`
- 阅读后发现：
  - ✅ **已有 `files` 字段**：`files: list[dict[str, Any]] | None = None`
  - 注释说明：每个 dict 对应 `UploadedFileInfo` 的序列化
  - 字段包括：`file_name`, `file_path`, `file_type`, `file_size`

**理解程度**：⭐⭐⭐⭐⭐
- 代码非常清晰，注释完整
- 一眼看出数据结构

---

### 第 2 步：理解文件存储位置

**执行的命令**：
```bash
find agents_hub/ -name "*file*.py" -o -name "*upload*.py"
grep -r "file_path\|upload" agents_hub/ --include="*.py"
```

**发现**：
- 找到 `agents_hub/api/services/file_service.py`
- 阅读后发现：
  - 存储路径：`{data_path}/teams/{team_id}/{group_chat_id}/file_snapshots/`
  - 文件名生成：`{原文件名}_{时间戳}_{UUID前16位}.{扩展名}`
  - 路径规范：相对于 `config.data_path` 存储

**理解程度**：⭐⭐⭐⭐
- 路径规范清晰
- 但有困惑：为什么是 `file_snapshots` 目录？（名字暗示"快照"，但实际是上传文件）

**困惑点**：
1. ❓ `file_snapshots` 这个名字的语义不符（上传文件不是快照）
2. ❓ 路径中的 `team_id` 从哪来？（`GroupChatService.upload_file` 中没看到传递 `team_id`）

---

### 第 3 步：理解前端如何发送消息

**执行的命令**：
```bash
find frontend/src -name "*ChatInput*"
grep -r "sendMessage\|onSend" frontend/src/layouts/ChatArea/
```

**发现**：
- 找到 `frontend/src/layouts/ChatArea/ChatInput.tsx`
- 阅读后发现：
  - 组件接收 `onSend: (text: string, files?: UploadedFileInfo[]) => void`
  - 使用 `useFileUpload` hook 管理文件上传
  - 调用链：`ChatInput.onSend` → `ChatArea.handleSend` → `sendMessage(activeSessionId, { content, members, files })`

**理解程度**：⭐⭐⭐⭐
- 前端数据流清晰
- 但需要跨 3 个文件才能拼凑完整流程

**困惑点**：
1. ❓ `sendMessage` 的具体实现在哪？（需要继续追踪到 `core/api/groupChatApi.ts`）
2. ❓ 前端如何先上传文件，再发送消息？还是一起发送？

---

### 第 4 步：理解后端如何接收消息

**执行的命令**：
```bash
grep -A 30 "async def send_message" agents_hub/api/routes/group_chat.py
grep -A 50 "async def send_message" agents_hub/api/services/group_chat_service.py
```

**发现**：
- 路由：`POST /group-chats/{group_chat_id}/messages`
  - 接收 `MessageCreate` schema，包含 `files: list[UploadedFileInfo] | None`
- Service 层：
  - 解析 `send_to`（从 `@成员名` 或默认 `manager`）
  - 激活群聊
  - 创建 `AgentCall`
  - 构建 `AgentMessage`（`files_dicts = [f.model_dump() for f in files]`）
  - 调用 `group_chat.send_message_to_agent(message)`

**理解程度**：⭐⭐⭐⭐⭐
- 后端数据流非常清晰
- 每一步都有日志和注释

**困惑点**：
1. ❓ 前端是先调用 `/upload` API 上传文件，拿到 `UploadedFileInfo`，再调用 `/messages` 发送消息？
2. ❓ 还是一个请求同时传文件和消息？（需要看前端 API 实现）

---

### 第 5 步：理解 Agent 如何处理附件

**执行的命令**：
```bash
grep -A 30 "async def _process_message" agents_hub/core/agent/base_agent.py
grep -A 30 "def build_user_prompt" agents_hub/core/context/agent_context.py
```

**发现**：
- `Agent._process_message` 方法接收 `AgentMessage`
- 调用 `agent_context.build_user_prompt(msg)` 构建完整 prompt
- `build_user_prompt` 包含 3 部分：
  1. Runtime 信息（agent 状态、调用记录）
  2. 历史上下文（`await self.get_context()`）
  3. Incoming message（`render_for_llm(msg)`）

**理解程度**：⭐⭐⭐
- 知道有这 3 部分，但不知道 `msg.files` 在哪里处理
- 需要继续追踪 `render_for_llm` 或 `AgentBridge.execute_stream`

**困惑点**：
1. ❓❓❓ **最大困惑**：`msg.files` 在哪里被读取并传递给 LLM？
2. ❓ 图片如何传递？（多模态 API 需要 base64？还是文件路径？）
3. ❓ 文本文件如何传递？（注入到 prompt？还是单独参数？）
4. ❓ `AgentBridge.execute_stream` 的接口是什么？只有 `prompt: str` 参数吗？

---

### 第 6 步：尝试追踪 `render_for_llm`

**执行的命令**：
```bash
grep -r "def render_for_llm" agents_hub/core/foundation/
```

**发现**：
- 没有找到 `render_for_llm` 的具体实现（应该在 `foundation/renderer.py`）
- 但从导入语句 `from agents_hub.core.foundation import render_for_llm` 可以推断存在

**尝试阅读 `AgentBridge`**：
- `execute_stream(prompt: str, config, session_id, cwd, fork_from, system_prompt)`
- 参数只有 `prompt: str`，没有 `files` 参数

**理解程度**：⭐
- 完全不理解 `msg.files` 如何传递到 LLM
- 代码中没有明显的线索

**困惑点**：
1. ❓❓❓ **核心困惑**：`msg.files` 字段在哪里被消费？
2. ❓ `render_for_llm(msg)` 是否会处理 `msg.files`？（需要阅读实现）
3. ❓ 还是在 `AgentBridge` 内部处理？（但接口没有 `files` 参数）
4. ❓ 还是在 `agent_context.build_user_prompt` 中拼接到 prompt 里？

---

## 2. 容易理解的内容（代码已说清楚）

### 2.1 数据结构
✅ `AgentMessage.files` 字段定义清晰  
✅ `UploadedFileInfo` schema 清晰（`file_name`, `file_path`, `file_type`, `file_size`）  
✅ 前端 `useFileUpload` hook 的逻辑清晰  
✅ 后端 API 路由和 Service 层的数据流清晰  

### 2.2 文件存储
✅ 存储路径规范：`{data_path}/teams/{team_id}/{group_chat_id}/file_snapshots/`  
✅ 文件名生成规则清晰  
✅ 路径安全校验（`_validate_path` 防止路径遍历）  

### 2.3 前端到后端的流程
✅ 前端先上传文件 → 拿到 `UploadedFileInfo` → 发送消息时携带  
✅ 后端接收消息 → 构建 `AgentMessage` → 投递到 `MessageRouter`  

---

## 3. 难以理解的内容（需要文档）

### 3.1 Agent 如何接收并处理附件？

**问题**：
- ❓❓❓ `msg.files` 字段在 Agent 中的哪里被读取？
- ❓ 图片如何传递给 LLM？（多模态 API 的调用方式）
- ❓ 文本文件如何传递给 LLM？（注入到 prompt？单独参数？）

**需要跨越的模块**：
- `Agent._process_message` → `agent_context.build_user_prompt` → `render_for_llm` → `AgentBridge.execute_stream` → `ClaudeExecutor.execute`

**为什么难理解**：
- 需要阅读 5+ 个文件才能拼凑完整流程
- `render_for_llm` 的实现没找到
- `AgentBridge.execute_stream` 的接口只有 `prompt: str`，看不出如何传递文件

---

### 3.2 不同类型文件的处理策略

**问题**：
- ❓ 图片：是否使用多模态 API？如何编码（base64？文件路径？）
- ❓ 文本文件（.txt, .md, .py）：是否直接读取内容注入到 prompt？
- ❓ PDF/Word：是否支持？如何提取文本？
- ❓ 压缩包：是否支持？如何处理？

**为什么难理解**：
- 代码中只看到文件类型白名单（`ALLOWED_CONTENT_TYPES`）
- 但没看到针对不同类型的处理逻辑

---

### 3.3 文件路径的传递链路

**问题**：
- ❓ `file_path` 是相对路径还是绝对路径？
- ❓ Agent 如何读取文件内容？（直接读取磁盘？还是通过 API？）
- ❓ 如果 Agent 在 Docker 容器中，如何访问宿主机文件？

**为什么难理解**：
- `UploadedFileInfo.file_path` 存的是相对路径（相对于 `data_path`）
- Agent 需要拼接完整路径才能读取
- 但代码中没看到这个拼接逻辑

---

### 3.4 `render_for_llm` 的职责边界

**问题**：
- ❓ `render_for_llm(msg)` 是否会处理 `msg.files`？
- ❓ 还是只处理 `msg.content`，把 `files` 留给其他模块？
- ❓ 如果处理，输出格式是什么？（纯文本？XML？JSON？）

**为什么难理解**：
- 函数实现没找到
- 注释说"渲染为 LLM 输入"，但不知道具体做了什么

---

### 3.5 `AgentBridge` 的接口设计

**问题**：
- ❓ `execute_stream(prompt: str, ...)` 只接收字符串 prompt
- ❓ 如果需要传递图片（多模态），接口如何支持？
- ❓ 是否需要修改 `AgentBridge` 接口？

**为什么难理解**：
- 接口看起来不支持传递文件
- 但功能已经存在（`msg.files` 字段已定义）
- 矛盾：数据结构支持，但接口不支持

---

## 4. 推荐的文档形式

基于实际探索经验，我认为需要以下文档：

### 4.1 数据流图（必需）

**形式**：Mermaid 流程图

**内容**：
```
前端上传文件 → UploadedFileInfo
    ↓
前端发送消息（携带 files）
    ↓
后端接收（MessageCreate）
    ↓
构建 AgentMessage（files 字段）
    ↓
Agent._process_message
    ↓
build_user_prompt ← 这里如何处理 files？
    ↓
render_for_llm ← 是否处理 files？
    ↓
AgentBridge.execute_stream ← 如何传递 files？
    ↓
ClaudeExecutor.execute ← 多模态 API 调用？
```

**为什么需要**：
- 单看代码需要跨 10+ 个文件
- 数据在每一层如何转换？（dict → Pydantic → dict → str？）
- 哪一层负责读取文件内容？

---

### 4.2 文件类型处理策略表（必需）

**形式**：Markdown 表格

| 文件类型 | 处理方式 | 传递给 LLM 的形式 | 代码位置 |
|---------|---------|------------------|---------|
| 图片（jpg/png） | 读取为 base64 | 多模态 API（`image` 块） | `xxx.py:L123` |
| 文本文件（.txt/.md） | 读取内容 | 注入到 prompt（`<file>` 标签） | `xxx.py:L456` |
| PDF | 提取文本 | 注入到 prompt | `xxx.py:L789` |
| 代码文件（.py/.js） | 读取内容 | 注入到 prompt（带语言标记） | `xxx.py:L012` |

**为什么需要**：
- 代码中只看到白名单，看不到处理逻辑
- 不同类型需要不同的读取和编码方式
- 如果要支持新类型，应该改哪里？

---

### 4.3 关键函数说明（必需）

**形式**：类似 API 文档的注释

```python
def render_for_llm(msg: AgentMessage) -> str:
    """渲染 AgentMessage 为 LLM 输入字符串
    
    处理逻辑：
    1. 基础信息：call_id, send_from, content
    2. 如果有 msg.files：
       - 图片类型：转为 <image src="..."> 标签
       - 文本类型：读取内容，包裹在 <file name="..."> 标签内
       - 其他类型：只显示文件名和路径
    
    返回格式：
    ```
    <incoming_message call_id="...">
    <from>user</from>
    <content>...</content>
    <files>
      <file name="example.txt" type="text/plain">
        文件内容...
      </file>
    </files>
    </incoming_message>
    ```
    """
```

**为什么需要**：
- 这个函数是数据转换的关键节点
- 看代码看不出具体行为
- 注释不足以说明复杂的处理逻辑

---

### 4.4 架构决策记录（建议）

**形式**：ADR（Architecture Decision Record）

**内容**：
- **为什么 `files` 是 `list[dict]` 而不是 `list[UploadedFileInfo]`？**
  - 决策：为了避免循环依赖（`foundation` 不依赖 `api.schemas`）
  - 权衡：牺牲类型安全，换取模块独立
  
- **为什么存储在 `file_snapshots` 目录？**
  - 决策：复用快照存储机制
  - 权衡：命名不直观，但统一管理

- **为什么 `AgentBridge.execute_stream` 只接收 `prompt: str`？**
  - 决策：文件已在 `render_for_llm` 中处理为文本
  - 权衡：接口简洁，但多模态支持受限

**为什么需要**：
- 解释"为什么这样设计"，不只是"怎么用"
- 避免未来重构时踩坑

---

## 5. 总结：AI 需要什么上下文？

### 5.1 快速定位（最紧急）

如果让我现在马上实现"支持 Agent 处理图片"，我最需要的是：

1. **一张流程图**：从 `msg.files` 到 LLM 的完整路径
2. **一行代码位置**：`msg.files` 在哪里被读取（文件名 + 行号）
3. **一个示例**：已有的文本文件处理逻辑在哪（可以照着改）

**为什么**：
- 代码太分散，跨 10+ 个文件
- 搜索 `msg.files` 只能找到定义和赋值，找不到消费点
- 需要"导航索引"快速定位关键代码

---

### 5.2 深入理解（次要）

如果要完整理解并维护这个功能，我需要：

1. **模块职责边界**：哪些模块负责什么（表格形式）
2. **数据转换规则**：每一层如何转换数据（流程图 + 代码示例）
3. **扩展点说明**：如果要支持新文件类型，应该改哪里

---

### 5.3 避免踩坑（重要）

如果要避免引入 bug，我需要：

1. **已知陷阱**：路径拼接、Docker 环境、文件大小限制
2. **测试用例**：已有的测试覆盖了什么场景
3. **回滚预案**：如果改错了，如何快速恢复

---

## 6. 反思：文档应该是什么样？

### 6.1 不是"教科书式的完整说明"

❌ **不需要**：
- 从零开始讲"什么是消息"、"什么是 Agent"
- 把每个函数都详细解释一遍
- 面面俱到的架构图（太大，看不懂）

✅ **需要**：
- 直接给出"核心数据流"
- 标注"关键跳转点"（哪个函数调用哪个）
- 只解释"代码看不出来的部分"

---

### 6.2 不是"事后总结"

❌ **不需要**：
- 功能已实现后，写一份"功能说明书"
- 罗列所有相关文件（这个 grep 就能做到）

✅ **需要**：
- **在实现前**，说清楚"应该怎么实现"
- **在实现后**，说清楚"为什么这样实现"
- 更新频率：代码改动时同步更新

---

### 6.3 应该是"导航索引 + 决策记录"

**理想形式**：

1. **导航索引**（快速定位）
   - 流程图（数据流）
   - 表格（模块 → 职责 → 代码位置）
   
2. **决策记录**（深入理解）
   - 为什么这样设计？
   - 权衡了什么？
   - 有什么已知问题？

3. **扩展指南**（实际操作）
   - 如果要支持 X，应该改哪里？
   - 有哪些测试需要补充？

---

## 7. 最终建议

如果只能提供一份文档，我希望是：

**《文件上传功能 - 数据流图 + 关键代码索引》**

**内容**：
1. 一张 Mermaid 流程图（前端 → 后端 → Agent → LLM）
2. 每个节点标注：模块名、函数名、行号
3. 关键转换点的代码片段（10 行以内）
4. 常见问题 FAQ（为什么看不到 `msg.files` 的消费点？）

**为什么**：
- 80% 的探索时间花在"找代码在哪"
- 20% 的时间花在"理解逻辑"
- 需要一份"地图"，而不是"百科全书"

---

## 附录：探索过程的痛点记录

1. **搜索无效**：`grep "msg.files"` 只能找到定义，找不到使用
2. **跨文件跳转**：需要手动追踪 10+ 次函数调用
3. **隐式约定**：`file_path` 是相对路径，但代码没明说
4. **缺失实现**：`render_for_llm` 找不到源码
5. **接口矛盾**：`msg.files` 存在，但 `AgentBridge` 接口不支持

**如果有文档，这些痛点都能避免。**
