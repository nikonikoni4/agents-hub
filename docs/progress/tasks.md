# 任务列表

> 此文件由 progress-tracker 自动维护，请勿手动编辑

---

## 待开始

### 12. 单聊功能
- **来源**：idea #10
- **状态**：⏳ 待开始
- **创建时间**：2026-06-06
- **描述**：增加单聊，分为两种：1. 完全与群组无关，即直接与一个 agent 聊天。2. 与群相关，因为每个群的每个 agent 都有自己的 session ID，与群相关的就是 consume 或 fork 这个 session ID。展现形式有两种：1. 像现在这样在聊天主界面进行单聊对话；2. 群聊加单聊，即在群聊中点击成员后进入该角色的单聊。
- **优先级**：11

### 13. 创建系统 Skill - Manager 工具
- **来源**：idea #5
- **状态**：⏳ 待开始
- **创建时间**：2026-06-06
- **描述**：给 Manager 安排创建成员和创建团队的工具，以及如何创建团队和成员的 Skill。同时给 Manager 一个工具，让他把这些创建的成员或者团队拉进群里。一个工具加一个 Skill。流程：新会话只有 Manager → 告诉他要创建新团队和群聊 → Manager 输入成员列表（可已有或新建）→ 新建需包括名称、描述 → 系统自动拉人进群。
- **优先级**：12

### 14. 远程连接
- **来源**：idea #11
- **状态**：⏳ 待开始
- **创建时间**：2026-06-06
- **描述**：添加远程连接，主要是参考 OpenClaw 那样直接远程连接飞书比较简单，然后通过 Slash Command 进行一些基本的任务
- **优先级**：13

### 15. 设计跨会话记忆
- **来源**：用户直接指定
- **状态**：⏳ 待开始
- **创建时间**：2026-06-06
- **描述**：设计 Agent 的跨会话记忆机制，使 Agent 能够在不同会话之间保留和复用知识
- **优先级**：14

### 16. 添加控制台
- **来源**：用户直接指定
- **状态**：⏳ 待开始
- **创建时间**：2026-06-06
- **描述**：添加控制台功能
- **优先级**：15

### 17. WebSocket 身份验证
- **来源**：用户直接指定
- **状态**：⏳ 待开始
- **创建时间**：2026-06-06
- **描述**：为 WebSocket 连接添加基于 Token 的身份验证，在握手阶段验证客户端身份
- **优先级**：16

### 19. Agent Git Worktree 隔离 ⭐
- **来源**：用户直接指定
- **状态**：⏳ 待开始
- **创建时间**：2026-06-07
- **描述**：当 Agent 使用 Docker 模式且其 cwd 与群聊目录一致时，前端弹出提示是否创建 git worktree。确认后后端创建 worktree 并将该 agent 的 cwd 切换到 worktree 路径，实现目录隔离。前端成员列表需显示 agent 的实际工作路径（与群聊路径不同时高亮），后端提示词注入需包含 cwd 信息让 agent 知道自己在独立目录工作。
- **优先级**：18
- **涉及模块**：
  - 前端：成员列表（RightSidebar）显示 cwd 路径、worktree 创建确认弹窗
  - 后端：新增 worktree 创建 API、AgentMemberInfo 增加 worktree 路径字段、提示词注入 cwd 信息
  - MCP：可能新增 create_worktree 工具让 agent 自主请求隔离

### 20. 群聊成员列表增强显示 ⭐
- **来源**：用户直接指定
- **状态**：⏳ 待开始
- **创建时间**：2026-06-07
- **描述**：增强群聊成员列表和聊天区域的显示信息，包含以下子项：
  1. **成员路径显示**：每个 Agent 成员需显示其 cwd 工作路径
  2. **群聊路径显示**：聊天栏顶部应显示当前群聊的项目路径
  3. **成员忙碌状态**：每个 Agent 增加 busy/idle 状态字段，前端成员列表需展示该状态（如忙碌中/空闲）
  4. **修复时间显示**：群聊列表的"XX小时前"时间显示有误，应为上次观看时间（last_view_at），当前显示逻辑错误需修正
- **优先级**：19
- **涉及模块**：
  - 前端：RightSidebar 成员列表（显示路径+状态）、ChatArea 顶部（群聊路径）、SessionItem（修复时间显示）
  - 后端：AgentMemberInfo 增加 status 字段、群聊 API 返回成员 cwd 和状态、last_view_at 逻辑修正

### 21. Pin 前后端连续点击 Bug
- **来源**：用户直接指定
- **状态**：⏳ 待开始
- **创建时间**：2026-06-07
- **描述**：当前 Pin 功能在前后端存在 Bug，连续快速点击 Pin/Unpin 按钮时可能触发错误。需要排查连续点击时的状态竞态问题，确保操作幂等且前端状态与后端一致。
- **优先级**：20
- **涉及模块**：
  - 前端：Pin 按钮点击事件防抖/节流、usePinnedMessages hook 状态同步
  - 后端：pin/unpin 端点的并发处理

### 22. Agent 任务中断后无法自动闭环
- **来源**：用户直接指定
- **状态**：⏳ 待开始
- **创建时间**：2026-06-07
- **描述**：当 Agent 正在执行任务时，如果后端被切断/重启，虽然 Agent call 状态仍为 running，但由于重启后没有新的消息来激活 Agent 的事件循环，导致任务无法完成闭环。系统需要一种机制来检测并恢复这些"悬空"的 running 状态任务，例如：重启时扫描未完成的 agent call，主动触发恢复流程或将其标记为异常中断。
- **优先级**：21
- **涉及模块**：
  - 后端：agent call 状态管理、重启恢复逻辑、event loop 激活机制
  - 前端：中断任务的状态展示与用户提示

### 23. GroupChatRuntimeState 并发竞态条件修复
- **来源**：docs/history-bugs/2026-06-05-group-chat-runtime-state-concurrency.md
- **状态**：⏳ 待开始
- **创建时间**：2026-06-07
- **描述**：`GroupChatRuntimeState` 及其关联的 `GroupChatRuntime` 在多协程并发访问时缺乏内存状态保护。Repository 层虽有 `asyncio.Lock` 保护文件 I/O，但 Runtime 层的 read-modify-write 序列存在竞态条件，可能导致持久化数据不一致。具体问题包括：
  1. `append_compact_record_and_mark_compacted` 双次 persist 非原子，中间失败会导致数据不一致
  2. `compact_messages` 存在 TOCTOU 问题，LLM 调用期间快照过时
  3. `AgentCallManager._calls` 无并发保护，`_cleanup_loop` 与主流程竞态
  4. `GroupChatManager._group_chats` 无锁（对比 `_tokens` 有 `threading.RLock`）
- **优先级**：22
- **涉及模块**：
  - `agents_hub/core/context/group_chat_runtime.py`：添加 `_state_lock` 保护所有 command 方法
  - `agents_hub/core/context/group_chat_context.py`：锁内读取快照并校验版本
  - `agents_hub/core/communication/agent_call_manager.py`：`_calls` 添加 `asyncio.Lock`
  - `agents_hub/core/orchestration/group_chat_manager.py`：`_group_chats` 添加 `threading.RLock`

### 24. 前端文件上传发送给 Agent ⭐
- **来源**：用户直接指定
- **状态**：⏳ 待开始
- **创建时间**：2026-06-07
- **描述**：前端需要支持用户上传文件并发送给 Agent。用户可以在聊天输入框中选择、粘贴或拖拽文件，文件随消息一起发送，Agent 能够接收并处理文件内容。当前支持的文件类型：
  1. **图片类型**：png、jpg、jpeg、gif、webp 等，Agent 通过多模态能力理解图片内容
  2. **文本类型**：txt、md、json、yaml、csv、py、js、ts 等代码/文本文件，Agent 读取文本内容
  - 暂不支持二进制类型（pdf、docx、zip 等）
- **优先级**：23
- **涉及模块**：
  - 前端：ChatInput 组件增加文件上传/粘贴/拖拽功能、消息气泡中文本文件预览和图片展示、上传进度与预览
  - 后端：文件上传 API、文件存储管理、消息中附件的数据结构（区分图片/文本类型）
  - Agent：图片通过多模态能力传递给 LLM、文本文件注入到上下文

### 27. 群聊管理：删除 / Fork / Compact
- **来源**：用户直接输入
- **状态**：⏳ 待开始
- **创建时间**：2026-06-08
- **描述**：群聊的生命周期管理功能，包括删除、fork、整体 compact 和单个成员 compact
- **优先级**：27
- **需求清单**：
  1. **群聊删除**：删除整个群聊及其关联数据
  2. **群聊 Fork**：复制群聊当前状态（成员、配置等），创建独立新群聊
  3. **整体 Compact**：压缩群聊内所有 agent 的上下文/状态及群聊级别数据
  4. **单个成员 Compact**：对群聊内的单个 agent 进行上下文压缩
- **待明确**：
  - [ ] fork 时需要复制哪些数据？
  - [ ] compact 触发方式（手动/自动/阈值）
  - [ ] 删除是软删除还是硬删除？

### 28. 群聊成员状态与路径显示优化
- **来源**：用户直接输入
- **状态**：⏳ 待开始
- **创建时间**：2026-06-08
- **描述**：增强群聊面板的成员信息展示（低优先级优化）
- **优先级**：28
- **需求清单**：
  1. **上下文统计与报警**：显示每个成员的上下文 token 使用量，超过 150K / 200K 时发出警告
  2. **路径显示**：群聊显示自身路径；成员若 worktree 路径与群聊相同用标识符标注（如 `≈`），不同则显示完整路径
  3. **打开工作目录**：支持打开每个成员的工作目录

### 29. Agent Bridge System Prompt 注入模式 ⭐
- **来源**：用户直接指定
- **状态**：⏳ 待开始
- **创建时间**：2026-06-08
- **描述**：修改 Agent Bridge，支持通过 system prompt 参数直接注入系统提示词，替代当前向 cloud.md 和 agents.md 文件写入的方式。这是多 Agent 使用同一个 Cloud MD 时状态冲突问题的解决方案。
- **优先级**：29
- **背景**：
  - 当前实现：系统提示词通过 `_inject_tool_usage_to_files` 方法写入 CLAUDE.md/AGENTS.md 文件
  - 问题：多个 Agent 并行使用同一个 Cloud MD 时，会产生文件读写竞态条件和状态冲突
  - 解决方案：支持通过 system prompt 参数直接注入，避免文件层面的冲突
- **需求清单**：
  1. **保留现有实现**：保留当前 `_inject_tool_usage_to_files` 方法，不删除
  2. **新增 system prompt 参数**：在 Agent 配置或初始化时增加 `system_prompt` 参数
  3. **实现注入方法**：新增方法将提示词内容直接拼接到 system prompt 中
  4. **切换机制**：实现一个配置开关或方法，允许在文件注入和 system prompt 注入两种模式间切换
  5. **默认行为**：建议默认使用 system prompt 模式，文件注入模式作为回退方案
- **涉及模块**：
  - `agents_hub/core/agent/base_agent.py`：修改 `_generate_tool_usage_content` 输出目标
  - `agents_hub/core/agent/bridge.py`：增加 system prompt 参数传递
  - 配置层：增加注入模式的配置选项
- **技术方案**：
  - 模式 A（当前）：`_inject_tool_usage_to_files` → 写入 CLAUDE.md/AGENTS.md
  - 模式 B（新增）：`_generate_system_prompt_content` → 拼接到 system prompt 参数
  - 切换方式：Agent 配置中的 `prompt_injection_mode: "file" | "system_prompt"`

### 30. Agent 质量提升四件套 ⭐
- **来源**：用户直接输入
- **状态**：⏳ 待开始
- **创建时间**：2026-06-09
- **背景**：CLI 模式下 Agent 表现"降智"——疯狂快速完成任务、缺乏架构理解、写 bug 多、审查不上心。根因：提醒机制缺少冷静期催促闭环、系统提示词引导不足、MCP 连接失败时无兜底机制、缺乏结构化的审查和上下文压缩流程。
- **优先级**：26
- **需求清单**：
  1. **系统提示词优化**：优化 Manager/Worker 的 CLAUDE.md，引导 Manager 在 call_agent 时提供足够上下文（相关文件、架构约束、验收标准），引导 Worker 先理解再动手
  2. **延缓闭合提醒**：当前 `_enqueue_finish_agent_call_reminder` 在消息处理完成后立即触发，打断 Agent 执行节奏。需加入冷静期（1-2 分钟延迟）再催促，同时优化提醒 prompt 措辞，从"快点闭环"改为"确保质量再闭环"
  3. **MCP 连接失败检测与兜底**：当 agents-hub MCP 服务不可用时，Agent 会反复尝试调用陷入内部循环。需添加健康检查 + fallback prompt（"无法连接 MCP，请直接用文字回复"），阻断无限重试
  4. **Hook 机制（before/after agent call）**：
     - `before_agent_call`：上下文压缩（会话轮转）、注入额外上下文、检查前置条件、熔断（连续失败拦截）
     - `after_agent_call`：自动审查、结果质量评分、触发后续流程、审计日志
- **Hook 上下文压缩详细设计**：
  - 核心思路：CLI 平台没有原生 compact API，通过会话轮转实现上下文压缩
  - 流程：`before_agent_call` 触发 → 检测当前会话上下文大小 → 超阈值时：
    1. 在当前会话中执行 hand-off（让 LLM 自行压缩上下文 → 输出 md 摘要文档）
    2. 创建新 CLI 会话（新 session_id）
    3. 新会话执行 hand-on（读取摘要文档 + system_message，恢复上下文）
    4. 更新 main_session_id
    5. 原始消息在新会话中继续执行
  - 已解决：上下文大小检测、hand-off 执行方式、跨平台抽象
  - 待解决：前端 session_id 变化时的展示问题
- **子任务拆分**：
  - 30.1 MCP 连接失败检测（优先级最高，防御性）
  - 30.2 延缓闭合提醒（改动小，效果直接）
  - 30.3 系统提示词优化（可持续迭代）
  - 30.4 Hook 机制设计与实现（架构级改动，收益最大）

---

## 进行中

（暂无）

## 已完成

### 26. 群聊消息压缩机制激活与上下文保护 ⭐
- **来源**：用户直接指定
- **状态**：✅ 已完成
- **创建时间**：2026-06-08
- **完成时间**：2026-06-08
- **描述**：当前 `compact_messages` 整条链路已实现但从未被触发，导致群聊消息无限累积，Agent 上下文被大量重复/低价值消息侵占。需要：
  1. **激活压缩机制**：找到合适的触发点（消息数量阈值 / 定时 / Agent 调用前），调用 `group_chat.compact_history()`
  2. **上下文保护**：压缩后 Agent 只加载增量摘要 + 未压缩新消息，避免重复内容侵占 token
  3. **去重能力**：压缩过程中需处理语义重复消息（如同一人对同一目标重复发送相似内容），利用 LLM 合并去重
  4. **触发策略**：需设计触发条件，如消息数超过 N 条、单次上下文 token 超限等
- **优先级**：25
- **相关文档**：
  - `docs/design-decisions/0010-agent-context-and-prompt-architecture.md`
  - `docs/design-decisions/0011-agent-tool-semantics-and-blocking-rules.md`

### 4. WebSocket 群聊实时刷新
- **来源**：idea #1
- **状态**：✅ 已完成
- **创建时间**：2026-06-06
- **完成时间**：2026-06-07
- **描述**：正式使用 WebSocket，每次添加群聊时都向前端发送消息，让前端刷新群聊信息
- **优先级**：3
- **完成内容**：
  - realtime 模块已实现 WebSocket 连接管理和房间模型
  - 前端 hooks（useAgentCalls、useTasks、usePinnedMessages 等）通过 WebSocket RefreshSignal 自动刷新
  - 后端 broadcast API 支持群聊级别的刷新信号推送

### 5. 消息 Pin ⭐
- **来源**：idea #13
- **状态**：✅ 已完成
- **创建时间**：2026-06-06
- **完成时间**：2026-06-07
- **描述**：消息 pin 会做一个明确的任务固定信号，会一直发给接下来每个 agent 的信息发送。并且在群聊中应该有一个显示方式，可以独立起来。
- **优先级**：4
- **完成内容**：
  - 后端：pin/unpin RESTful 端点、pins.json 持久化、幂等操作
  - 前端：hover pin 按钮、usePinnedMessages hook、右侧栏 Pinned 模块
  - Pin 消息注入到 Agent 提示词上下文（base_agent.py）
  - Spec 文档：docs/specs/2026-06-06-pinned-messages.md

### 6. 消息回复/引用 ⭐
- **来源**：idea #12
- **状态**：✅ 已完成
- **创建时间**：2026-06-06
- **完成时间**：2026-06-07
- **描述**：消息操作应该有回复、引用等可以选中某个消息，然后对它进行回复，回复的时候就会附带这个消息。
- **优先级**：5
- **完成内容**：
  - 前端 ChatArea 增加回复按钮和引用展示
  - ChatInput 支持回复状态（显示被引用消息）
  - Pin 消息注入到 Agent 上下文的工具使用说明
  - Spec 文档：docs/specs/2026-06-07-message-reply-quote.md
  - 测试：tests/unit/test_agent_pin_injection.py

### 7. 前端 Agent call 状态显示 ⭐
- **来源**：idea #6
- **状态**：✅ 已完成
- **创建时间**：2026-06-06
- **完成时间**：2026-06-07
- **描述**：在前端添加 Agent 的 call 状态。当前后端已有完整的 Agent call 状态，需在前端补充。需要先思考展示方式，再进行实现。
- **优先级**：6
- **完成内容**：
  - 后端：新增 GET /group-chats/{id}/agent-calls 路由端点和 Schema
  - 前端：AgentCallsPanel 组件、useAgentCalls hook
  - 点击 call 项弹出详情弹窗（ID、状态、收发方、时间、内容、错误）
  - 右侧栏选项卡「任务」tab 下展示

### 8. 任务面板 ⭐
- **来源**：idea #7
- **状态**：✅ 已完成
- **创建时间**：2026-06-06
- **完成时间**：2026-06-07
- **描述**：当前已实现任务功能，但缺乏测试和实际使用场景。需在前端增加任务面板，以展现当前任务的实现情况。
- **优先级**：7
- **完成内容**：
  - 后端：新增 GET /group-chats/{id}/tasks 路由端点和 Schema
  - 前端：TasksPanel 组件、useTasks hook
  - 点击 task 项弹出详情弹窗（ID、状态、负责人、创建者、时间、内容）
  - 右侧栏选项卡「任务」tab 下展示

### 18. 前端添加内容后其他组件未更新的职责判定
- **来源**：用户直接指定
- **状态**：✅ 已完成
- **创建时间**：2026-06-07
- **完成时间**：2026-06-07
- **描述**：当前端添加了某个内容（如创建群聊、添加成员等）后，其他相关组件未同步更新信息。需要分析判断这个"通知其他组件刷新"的职责应归属前端还是后端。
- **优先级**：17
- **完成内容**：前端除了群聊消息刷新的相关操作区域，其余操作的刷新缓存都应该在前端实现

### 2. 提示词优化 - 任务执行
- **来源**：idea #4
- **状态**：✅ 已完成
- **创建时间**：2026-06-06
- **完成时间**：2026-06-06
- **描述**：针对完成特定任务，对系统进行专门的提示词优化
- **优先级**：1
- **完成内容**：
  - 优化 Manager 的 finish_agent_call 工具说明：强调安排任务后即可闭环，无需等待 Worker 完成
  - 优化 Worker 的 finish_agent_call 工具说明：强调完成实际工作后才闭环
  - 改进 Task 未闭环提醒：包含 call_id、来源、原始请求摘要，Manager 额外说明可提前闭环
  - 改进 Heartbeat 内容：Worker 停止时指示 Manager 通过 report_progress 向 user 说明
  - 添加 `[Agents Hub 平台消息]` 标识到 render_for_llm，让 Agent 识别消息来源平台
  - 更新 render_for_llm 测试用例适配新格式
  - 创建 Agent 提示词系统 spec 文档

### 1. 提示词优化 - 群聊工具使用
- **来源**：idea #3
- **状态**：✅ 已完成
- **创建时间**：2026-06-06
- **完成时间**：2026-06-06
- **描述**：编写如何使用 speak in the group 和 finish agent call 两个工具的使用场景，明确写在提示词里边。目前主要针对如何使用信息以及让系统回答更流畅。
- **优先级**：1
- **完成内容**：
  - 在 `base_agent.py` 中添加了 `_generate_tool_usage_content` 方法，区分 Manager/Worker 生成不同的工具使用说明
  - 添加了 `_inject_tool_usage_to_files` 方法，将工具使用说明注入到 CLAUDE.md/AGENTS.md 的 TOOL_USAGE 标记中
  - Manager 说明所有工具，Worker 只说明 report_progress 和 finish_agent_call
  - 说明了群聊消息显示规则和工具使用场景

### 3. 群聊信息显示问题排查
- **来源**：idea #2
- **状态**：✅ 已完成
- **创建时间**：2026-06-06
- **完成时间**：2026-06-06
- **描述**：排查当前群聊数据在群里面显示的问题。正常流程应该是 users @ manager，manager @ worker。每个 worker 应该先在群里说收到任务并开始执行，结束时使用 finish agent call @ manager 说任务已完成。当前有些地方没有 @，内容缺失。
- **优先级**：2
- **完成内容**：
  - 修改了 `group_chat.py` 的 `send_message_to_agent` 方法
  - 在保存消息前判断是否已有 @ 前缀，如果没有则调用 `render_for_chat` 格式化
  - 确保所有通过 `send_message_to_agent` 发送的消息都会被正确格式化

### 9. 代码 DIFF 功能
- **来源**：idea #8
- **状态**：✅ 已完成
- **创建时间**：2026-06-06
- **完成时间**：2026-06-07
- **描述**：实现代码的 DIFF，不仅限于代码，就是它修改内容的 DIFF
- **优先级**：8
- **完成内容**：
  - DiffViewer 组件已实现，支持代码差异对比展示
  - 支持预览渲染和代码高亮双主题

### 10. 预览功能
- **来源**：idea #9
- **状态**：✅ 已完成
- **创建时间**：2026-06-06
- **完成时间**：2026-06-07
- **描述**：添加上预览功能，需要集成浏览器的功能。比如说让它创建一个网页，网页可以直接在这个窗口中展示。预览应该可以包括文本类的和网站类的，可以是 localhost，也可以是 HTML。
- **优先级**：9
- **完成内容**：
  - 预览功能已集成，支持文本类和网站类内容展示

### 11. Agent 完成工作后的文档组件 ⭐
- **来源**：idea #14
- **状态**：✅ 已完成
- **创建时间**：2026-06-06
- **完成时间**：2026-06-07
- **描述**：当一个 Agent 完成了一个代码工作或文档编写，它应该发送一个组件，这个组件就是它发送的文档的名字以及类型，点击它可以在右边的那个侧栏进行预览。并且这个组件还有几个固定的功能，比如重新生成、引用，然后添加批注再发给 Agent 让它重新生成。
- **优先级**：10
- **完成内容**：
  - 文档组件已实现，支持文档名称和类型展示
  - 点击可在右侧栏预览，支持重新生成、引用、批注功能

### 单元测试 - 注入函数幂等性测试
- **来源**：用户要求
- **状态**：✅ 已完成
- **创建时间**：2026-06-06
- **完成时间**：2026-06-06
- **描述**：为注入函数编写单元测试，重点测试幂等性，确保多次注入不会重复添加标签
- **完成内容**：
  - 创建了 `tests/core/agent/test_tool_usage_injection.py`
  - 测试了 `_generate_tool_usage_content` 生成内容（Manager/Worker）
  - 测试了 `_inject_tool_usage_to_files` 注入到文件
  - 测试了注入幂等性：多次注入不会重复添加标签
  - 测试了 `send_message_to_agent` 消息格式化逻辑
  - 测试了 `markdown_injector.replace_marked_section` 的幂等性
  - 所有 18 个测试通过

### 25. 前端侧栏抽屉按钮修复
- **来源**：用户直接指定
- **状态**：✅ 已完成
- **创建时间**：2026-06-07
- **完成时间**：2026-06-07
- **描述**：前端两个侧栏的抽屉按钮当前失效，需要修复：
  1. **左侧栏抽屉按钮**：顶栏左边的按钮，原本可以收起/展开左侧会话列表栏，当前点击无响应
  2. **右侧栏抽屉按钮**：聊天主界面右上方的按钮，原本可以收起/展开右侧栏，当前点击无响应
- **优先级**：24
- **涉及模块**：
  - 前端：顶栏 Header 组件（左侧栏按钮）、ChatArea 顶部（右侧栏按钮）、sidebar 状态管理
