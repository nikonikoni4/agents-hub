# E2E 测试问题记录

## ~~阻塞性问题（Blocker）~~ — 已关闭

### ~~B1: 创建团队按钮 disabled 属性未正确更新~~ — 非 bug
- **状态**: 已关闭（测试脚本选择器错误，非前端 bug）
- **原因**: 测试脚本点击了"团队名称"这个表单 label，而不是成员的 checkbox label。正确点击成员 label 后，checkbox 正常选中，按钮正常启用
- **验证**: `b1_01_after_click_label.png` 确认 bare_claude 选中、按钮可用

## 非阻塞性问题（Non-blocker）

### N1: 角色卡片无编辑/删除按钮
- **阶段**: Phase 2 - 角色管理
- **现象**: 角色列表中每个角色卡片上没有编辑或删除按钮
- **影响**: 无法通过 UI 编辑或删除角色
- **截图**: `phase2_03_role_list.png`

### N2: 创建团队对话框中成员标签选择反馈不明确
- **阶段**: Phase 3 - 团队管理
- **现象**: 点击成员标签后，视觉上有颜色变化，但 playwright 检测到按钮仍是 disabled，说明选中状态可能没有正确触发表单验证
- **截图**: `phase3_03b_selected_member.png`

### ~~N3: 发送消息 API 返回 400~~ — 已修复
- **状态**: 已关闭
- **原因**: 前端传 `send_to: ''`，后端校验空字符串不是成员
- **修复**: 改为前端传 `members` 列表，后端自行解析 `@` 确定 `send_to`（无 `@` 则发给 manager）
