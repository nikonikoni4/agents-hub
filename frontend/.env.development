# 开发环境配置
VITE_USE_MOCK=false
VITE_API_BASE_URL=/api/v1

# 端口配置（可修改以支持多分支并行测试）
VITE_DEV_PORT=5173
VITE_API_PORT=8099

# WebSocket URL（可选，留空则自动根据 VITE_API_PORT 构建）
# VITE_WS_BASE_URL=ws://localhost:8099/api/v1
